import React from "react";

const Section = () => {
  return (
    <div>
      <img src={images.hero} />
      <img src={images.company} />
      <img src={images.testimonial} />
    </div>
  );
};

export default Section;
